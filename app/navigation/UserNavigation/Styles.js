import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
    profileView:{
        flex:1,
        alignItems:"center"
    },
    likesView:{
        flex:1,
        alignItems:"center"
    },
    newsView:{
        flex:2,
        alignItems:"center"
    },
    wallView:{
        flex:1,
        alignItems:"center"
    }
});